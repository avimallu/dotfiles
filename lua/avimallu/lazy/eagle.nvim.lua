return {
  "soulis-1256/eagle.nvim",
  opts = {
    --override the default values found in config.lua
  },
  config = function()
    require("eagle").setup({
    --override the default values found in config.lua
    })
  end
}
